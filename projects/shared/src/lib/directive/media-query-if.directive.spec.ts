import { MediaQueryIfDirective } from './media-query-if.directive';

describe('MediaQueryIfDirective', () => {
  it('should create an instance', () => {
    const directive = new MediaQueryIfDirective();
    expect(directive).toBeTruthy();
  });
});
